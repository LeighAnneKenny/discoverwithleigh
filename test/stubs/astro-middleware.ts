// Stub for the astro:middleware virtual module: at runtime defineMiddleware
// is the identity function; only the type wrapper matters in Astro proper.
export const defineMiddleware = (fn: any) => fn;
